name: Biology App CI

on:
  push:
    branches:
      - 'main'
    paths:
      - 'app/bio/**'
      - '.github/workflows/app-bio-ci.yaml'
  pull_request:
    branches:
      - 'main'
    paths:
      - 'app/bio/**'
      - '.github/workflows/app-bio-ci.yaml'

jobs:
  build:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x, 22.x]

    steps:
      - uses: actions/checkout@v4
      - name: Use Node.js ${{ matrix.node-version }}
        uses: actions/setup-node@v4
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm'
      - run: npm ci
      - run: npm run build -w @nimbus-labs/deeptime
      - run: npm run check-format -w bio
      - run: npm run lint -w bio
      - run: npm run build -w bio
      - run: npm test -w bio
