# Algorithms

Landing page for compsci's algorithm visualizations. One of the
[compsci app's pages](../../../README.md#pages); shows a filtered view of the compsci home page,
listing only the algorithm sub-pages:

- [Sorting](sort/README.md)
- [Searching](search/README.md)

The list itself (`AlgorithmsList`) is shared with the home page's "Algorithms" section, so both stay
in sync as pages are added.
