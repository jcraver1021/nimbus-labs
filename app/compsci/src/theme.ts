import {createNimbusTheme} from '@nimbus-labs/ui';

// Blue: a technical, structured feel to match algorithm visualizations.
export const theme = createNimbusTheme({
  palette: {
    primary: {main: '#1565c0'},
  },
});
