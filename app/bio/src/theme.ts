import {createNimbusTheme} from '@nimbus-labs/ui';

/**
 * Green: an organic, natural feel to match biology/evolution content.
 */
export const theme = createNimbusTheme({
  palette: {
    primary: {main: '#2e7d32'},
  },
});
