import {createNimbusTheme} from '@nimbus-labs/ui';

/**
 * Brown: soil and sedimentary rock, to match digging down through the
 * geologic record.
 */
export const theme = createNimbusTheme({
  palette: {
    primary: {main: '#6d4c41'},
  },
});
